# Schema Version Map

**Status:** normative metadata.

| Protocol Version | Schema Set Version | Notes |
| --- | --- | --- |
| v0.5.x (current) | pending-backfill | Initial migration baseline; explicit schema files to be backfilled. |

Update this table whenever protocol-compatible schema sets change.
